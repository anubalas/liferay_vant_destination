package com.example.iotmonitoring.service;

import com.example.iotmonitoring.model.DeviceData;
import org.springframework.stereotype.Service;

/**
 * Service for evaluating alert rules based on incoming device data.
 */
@Service
public class AlertRuleEvaluationService {
    /**
     * Evaluates alert rules based on the incoming device data.
     * @param deviceData The incoming device data.
     */
    public void evaluateAlertRules(DeviceData deviceData) {
        // Implement alert rule evaluation logic here
    }
}